import { NodeSSH } from 'node-ssh'; const ssh = new NodeSSH(); async function runRemoteCheck() { try { await ssh.connect({ host: 'servidor.leanglobal.cl', port: 1295, username: 'root', password: 'lgbl2025.' }); console.log('Conectado'); const res = await ssh.execCommand('pm2 logs 10 --lines 50 --nostream'); console.log(res.stdout || res.stderr); } catch (err) { console.error('Error:', err); } finally { ssh.dispose(); } } runRemoteCheck();
